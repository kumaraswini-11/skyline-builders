import {cn} from "@/lib/utils";

export const Hero = () => {
  return (
    <section className={cn("relative h-[70vh]", "bg-hero bg-cover bg-center bg-no-repeat")}>
      {/* Overlay */}
      <div className=""></div>
    </section>
  );
};
